import React from "react";
import { Loader } from "semantic-ui-react";

// Load loader animation and export loader component
export default function Circle() {
  return <Loader active inline="centered" />;
}
